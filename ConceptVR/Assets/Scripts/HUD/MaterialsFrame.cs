using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MaterialsFrame : HUDFrame {
    /*
    Controller controller;

    HUDManager HUD;
    HUDFrame materialsFrame;

    // Use this for initialization
    void Start () {
        HUD = GameObject.Find("Managers").GetComponent<HUDManager>();

    }

    // Update is called once per frame
    void Update () {
        base.Update();
	}

    public override void OnEnable()
    {

        controller.changeTool("MaterialsTool");
        foreach (SwapToolButton b in transform.parent.GetComponentsInChildren<SwapToolButton>())
            if (b.toggled && b != this)
            {
                b.OnPress();
            }

        base.OnEnable();
    }

    public override void OnDisable()
    {
        base.OnDisable();
    }
    */
}
